streamlit==1.28.0
Pillow==10.0.0
matplotlib==3.7.1
numpy==1.24.3
opencv-python-headless==4.8.1.78
gTTS==2.3.2
ipython==8.15.0

# Choose ONE of these - not both:
# Option 1: TensorFlow (lighter)
tensorflow-cpu==2.13.0

# Option 2: PyTorch (lighter)
# torch==2.1.0
# torchvision==0.16.0

# Remove easyocr if possible - it's very heavy
# easyocr==1.7.1  # Only include if absolutely necessary